node_modules
dist
coverage
*.log
.env
.env.local
.DS_Store
.vscode
.idea

# Husky-managed internal files (regenerated by the `prepare` script)
.husky/_

# Local user-generated CLI config (created by `cra-ready init`)
cra-ready.yml
